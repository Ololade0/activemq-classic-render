<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:amq="http://activemq.apache.org/schema/core"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://activemq.apache.org/schema/core
           http://activemq.apache.org/schema/core/activemq-core.xsd">

    <!-- ActiveMQ broker definition -->
    <amq:broker brokerName="localhost"
                useJmx="true"
                persistent="false"
                dataDirectory="${activemq.data}">

        <!-- Messaging connectors -->
        <amq:transportConnectors>
            <amq:transportConnector name="openwire" uri="tcp://0.0.0.0:61616"/>
            <!-- Web console HTTP connector -->
            <amq:transportConnector name="web" uri="http://0.0.0.0:8161"/>
        </amq:transportConnectors>

    </amq:broker>

    <!-- Enable the embedded Jetty web console -->
    <bean id="jetty" class="org.apache.activemq.web.WebConsoleJettyServer">
        <property name="port" value="8161"/>
        <property name="host" value="0.0.0.0"/>
        <property name="jettyConfig" value="${activemq.conf}/jetty.xml"/>
    </bean>

</beans>
